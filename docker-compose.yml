services:
  grandstay-frontend:
    build:
      context: .
    container_name: grandstay-frontend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      FRONTEND_PORT: 5173
    ports:
      - "5173:5173"
